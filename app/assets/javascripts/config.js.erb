/*jslint sloppy: true */
/*global window: false */
(function (window) {
  /**
   * For security, it's best to remove this file, and store your API ID and
   * secret in a server-side API middleman endpoint.
   */
  window.config = {
    
    apiId:'<%= ENV['CMM_API_KEY'] %>',
    apiUrl:'<%= ENV['CMM_API_URL'] %>',
    version: 1
  };
}(window));
