<% flash.each do |key, messages| %>
  <% Array(messages).each do |message| %>
    <div class="<%= flash_class(key) %>" id="flash_<%= key %>">
      <button type='button' class='close' data-dismiss='alert'>&times;</button>
      <%= message %>
    </div>
  <% end %>
<% end %>
