require 'rails_helper'

RSpec.describe Pharmacy, type: :model do
  it 'has no need for tests - it is a lookup table' do
    # no tests for pharmacy needed because it's just a look up table
    true
  end
end
